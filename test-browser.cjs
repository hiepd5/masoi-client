const puppeteer = require('puppeteer'); (async () => { const browser = await puppeteer.launch(); const page = await browser.newPage(); page.on('console', msg => console.log('PAGE1 LOG:', msg.text())); page.on('pageerror', err => console.log('PAGE1 ERROR:', err.toString())); await page.goto('http://localhost:5000'); await page.waitForSelector('.btn-primary'); await page.click('.btn-primary'); await new Promise(r => setTimeout(r, 1000)); const code = await page.evaluate(() => document.querySelector('.room-code').innerText); console.log('Room created:', code); const page2 = await browser.newPage(); page2.on('console', msg => console.log('PAGE2 LOG:', msg.text())); page2.on('pageerror', err => console.log('PAGE2 ERROR:', err.toString())); await page2.goto('http://localhost:5000'); await page2.waitForSelector('.btn-secondary'); await page2.type('input[placeholder=\'VD: A3F9K\']', code); await page2.click('.btn-secondary'); await new Promise(r => setTimeout(r, 2000)); console.log('Done'); await browser.close(); })();
